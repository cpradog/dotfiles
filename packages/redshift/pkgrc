#!/usr/bin/env sh

import xdg files

is_available() {
  exists_command 'redshift'
}

install() {
  xdg_link_config 'redshift.conf' 'polybar/redshift.conf'
}

uninstall() {
  xdg_unlink_config 'redshift'
}
