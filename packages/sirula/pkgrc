#!/usr/bin/env sh

import xdg files

is_available() {
  exists_command 'sirula'
}

install() {
  xdg_link_config 'config.toml' 'sirula/config.toml'
  xdg_link_config 'style.css' 'sirula/style.css'
}

uninstall() {
  xdg_unlink_config 'sirula'
}
