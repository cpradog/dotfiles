#!/usr/bin/env sh

import xdg files

is_available() {
  exists_command 'waybar'
}

install() {
  xdg_link_config 'config' 'waybar/config'
  xdg_link_config 'style.css' 'waybar/style.css'
  xdg_link_config 'modules' 'waybar/modules'
}

uninstall() {
  xdg_unlink_config 'waybar'
}
