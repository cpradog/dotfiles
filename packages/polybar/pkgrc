#!/usr/bin/env sh

import xdg files

is_available() {
  exists_command 'polybar'
}

install() {
  xdg_link_config 'config' 'polybar/config'
  xdg_link_config 'startup.sh' 'polybar/startup.sh'
}

uninstall() {
  xdg_unlink_config 'polybar'
}
